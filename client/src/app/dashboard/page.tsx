import React from 'react'

const Dashboard = () => {
  return (
    <div>
      This is where the user will be able to see the status of the tasks, the progress of the tasks, the tasks that are pending, the tasks that are completed, etc. in a sleek and user-friendly UI.
    </div>
  )
}

export default Dashboard
